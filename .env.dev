# This is your local development environment variable file.
# The default values in settings.py are already configured for dev.
# You only need to add variables here if you want to override the dev defaults 
# (e.g. to test production-like settings locally, or for a custom REDIS_HOST).
#
# DEBUG=True
# CORS_ALLOWED_ORIGINS=http://localhost:5173
